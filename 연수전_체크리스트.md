# 연수 전 준비 · 현장 테스트 체크리스트

> 이 문서 = ① 주찬희(관리자)가 배포 전에 Firebase에서 해야 하는 설정, ② 폰 2대로 돌려보는 현장 테스트.
> 코드 수정은 이미 끝나 `main`에 올라가 있음. 아래 3가지 설정만 하면 로그인·보안·백업이 켜짐.

---

## 1. 주찬희가 직접 해야 하는 것 (Firebase 콘솔)

Firebase 콘솔: https://console.firebase.google.com → 프로젝트 **toky-trip**

### 1-1. 익명 인증 켜기  *(필수)*
1. 왼쪽 메뉴 **빌드 → Authentication** → **시작하기**
2. **Sign-in method** 탭 → **익명(Anonymous)** → 사용 설정 **On** → 저장

> 앱이 로드될 때 모두 자동으로 익명 로그인됨. 이건 접근 통제가 아니라, 아래 보안 규칙(`auth != null`)과 쓰기 검증을 켜기 위한 것.

### 1-2. 보안 규칙 교체  *(필수 · 반드시 1-1 다음에)*

> ⚠️ 순서 중요: **익명 인증(1-1)을 먼저** 켜야 함. 규칙만 먼저 바꾸면 로그인이 안 돼서 전원 접속 불가.

1. 왼쪽 메뉴 **빌드 → Realtime Database** → **규칙(Rules)** 탭
2. 내용을 전부 지우고 저장소의 [`firebase-rules.json`](firebase-rules.json) 안의 `{ "rules": { ... } }` 를 그대로 붙여넣기
3. **게시(Publish)**

바뀌는 것:
- 로그인(익명 인증) 안 된 요청은 읽기·쓰기 모두 차단 → 검색엔진·무작위 스크래핑 차단
- 공유/개인 메모 1건 최대 500KB, 이름 40자 제한 → "쓰레기 데이터 한 방에 전원 마비" 방지

### 1-3. (선택) Blaze 요금제 + 예산 알림
- 안 해도 익명 인증·규칙·백업은 **무료(Spark)로 동작**함
- 사진을 많이 넣어 월 다운로드 10GB를 넘길 것 같으면: **⚙️ → 사용량 및 결제 → 요금제 수정 → Blaze**, 그리고 **예산 알림 $5** 설정
- 넘겨도 앱이 죽는 게 아니라 그 달 나머지 읽기/쓰기가 막히는 것(Spark). Blaze면 초과분만 소액 과금.

### 1-4. 배포 후 첫 세팅 (주찬희 폰/PC)
1. https://godsjch-ops.github.io/tokyo-trip/ 접속 → 첫 화면에서 **주찬희** 입력 → 입장
2. 상단에 편집 버튼들이 보이면 관리자로 인식된 것 (`＋ 일정 추가`, `📋 PDF 일정표 불러오기` 등)
3. 연수 시작(9/14) 전이면 **📋 PDF 일정표 불러오기** 1회 실행 → `전면교체` 입력 → 공유 DB에 최신 일정 반영
   - 9/14부터는 이 버튼이 자동으로 사라짐(사고 방지). 그 뒤엔 개별 편집 또는 스냅샷 복원 사용.
4. 현장 모드 → **전체** 탭 아래쪽에 **관리자 · 접속 명단 / 스냅샷 복원**이 보이는지 확인

---

## 2. 참가자에게 안내할 것

- 링크: https://godsjch-ops.github.io/tokyo-trip/
- 첫 화면에서 **성을 포함한 실명**(예: 홍길동) 입력 → 입장. **한 번만** 입력하면 계속 유지됨.
- "명단에 없다"고 나오면 → 주찬희에게 연락 (주찬희가 현장 모드 "전체 → 접속 명단"에서 즉시 추가)
- 온라인일 때 **한 번은 꼭 접속**해 둘 것 (오프라인 캐시 생성 + 마지막 일정 저장)

---

## 3. 현장 테스트 체크리스트 (폰 2대 — A=관리자 주찬희, B=일반 참가자)

각 항목 통과하면 ☑. 실패하면 어떤 폰/브라우저였는지 메모해서 알려줘.

### 로그인 게이트
- [ ] B폰: 처음 열면 로그인 화면이 뜬다
- [ ] B폰: 명단에 없는 이름("test") → "명단에서 찾지 못했어요" 표시, 입장 안 됨
- [ ] B폰: 본인 실명 입력 → 입장, 앱이 보인다
- [ ] B폰: 앱을 완전히 껐다 다시 열어도 로그인 안 물어봄
- [ ] B폰: 오타 1글자("김선무"→김선모) 넣어도 입장되는지
- [ ] B폰: 편집 버튼(＋ 일정 추가 등)이 **안 보인다** (보기 전용)
- [ ] A폰: "주찬희"로 입장 → 편집 버튼이 **보인다**

### 명단 추가 (A폰)
- [ ] A폰: 현장 모드 → 전체 → 접속 명단 → 아무 이름 추가
- [ ] B폰(다른 기기): 그 이름으로 새로 로그인 시도 → 입장됨

### 동시 편집 (A폰 + 다른 관리자 기기, 또는 A폰 2개 탭)
- [ ] 기기1에서 9/15 블록 이동 + 기기2에서 9/17 블록 이동을 거의 동시에 → **둘 다 반영**되고 서로 안 사라짐
- [ ] 기기1에서 새 일정 추가 → 기기2 화면에 몇 초 안에 나타남

### 스냅샷 / 복원 (A폰)
- [ ] 전체 → 스냅샷 복원 → "💾 지금 상태 저장" → 목록에 방금 시각이 뜬다
- [ ] 일정 블록 하나 삭제 → 스냅샷 목록에서 방금 것 "되돌리기" → `되돌리기` 입력 → 삭제한 블록이 돌아옴
- [ ] 되돌린 뒤 목록에 "복원 전" 스냅샷이 새로 생겼다

### 사진 · 메모
- [ ] B폰(갤럭시): 공유 메모에 사진 추가 → 자르기 프레임 **모서리·변 손잡이가 사진 밖으로 안 나가고 잘 잡힌다**
- [ ] B폰: 공유 메모에서 사진 크기 옵션이 **작게 / 보통** 만 있다 (크게·원본 없음)
- [ ] B폰: 개인 메모 작성 → 앱 껐다 켜기 → 내용 유지
- [ ] A폰: B가 올린 공유 메모가 보이고, 기관/숙소 탭에 빨간 배지가 뜬다

### 오프라인 (B폰 — 비행기 모드)
- [ ] 온라인에서 한 번 연 뒤 → 비행기 모드 → 앱 다시 열기 → **일정표가 (마지막으로 받은 상태로) 보인다**
- [ ] 비행기 모드에서 상단에 주황색 "오프라인" 띠가 뜬다
- [ ] 비행기 모드에서 개인 메모 작성 → 비행기 모드 해제 → 잠시 후 클라우드에도 반영(다른 기기에서 확인 어려우면 생략)
- [ ] 비행기 모드에서 공유 메모 "올리기" → "신호 오면 자동으로" 안내 → 해제 후 자동 전송됨
- [ ] 비행기 모드에서 기관 시트 → "📄 기관자료 자세히보기" → PDF 열림 (미리 한 번 열어봤어야 함)

### PDF 일정표 불러오기 잠금
- [ ] (9/14 이후 테스트라면) A폰 상단에 "📋 PDF 일정표 불러오기" 버튼이 **없다**
- [ ] (9/14 이전이라면) 버튼 클릭 → `전면교체` 입력해야만 진행됨

---

## 4. 알려진 한계 (감수하기로 한 것)

- 이름 게이트는 브라우저(클라이언트) 검증이라, 11명 중 개발자도구를 다룰 줄 아는 사람이 마음먹으면 편집 우회 가능 → **스냅샷 복원**이 안전망
- `index.html` 소스는 GitHub·Pages에서 공개 → "소스 보기"하면 코드에 있는 연락처·명단이 보임 (검색 색인은 `noindex`로 차단). 로밍 번호 등은 어차피 팀 공유 예정이라 감수
- 익명 인증 계정이 기기·세션마다 쌓임 (무료 한도 내). 연수 후 콘솔에서 정리 가능
- Realtime Database는 웹에서 디스크 오프라인 저장이 없음 → 오프라인 시 "마지막으로 받은 일정"을 localStorage 캐시로 보여줌(위 테스트 항목). 한 번도 접속 안 한 기기는 오프라인에서 일정이 빈 채로 열림
